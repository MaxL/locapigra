= link_to cart_path, class: "btn btn-danger cart-btn", data: { toggle: "tooltip", placement: "auto bottom" }, title: "#{number_to_currency current_order.subtotal, locale: :de}" do
  %i.fa.fa-shopping-cart
  %span.badge= order_items_quantity
