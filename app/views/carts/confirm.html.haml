.container
  .row
    .col-xs-12.col-sm-offset-1.col-sm-10.col-md-offset-2.col-md-8.text-center
      %h1 Thank you for your order!
      %p Your purchase of:
      .row
        .col-xs-12.col-sm-12.col-md-offset-3.col-md-6
          %table.table.table-bordered
            - @order.order_items.each do |item|
              %tr
                %td.text-left
                  = item.quantity
                  x
                  = item.product.name
                %td.text-right= item.quantity * item.unit_price
      %p Will be shipped as soon as your payment is confirmed.
      %p If you chose to pay by bankwire, please wire the total amount of:
      %p
        %strong.text-danger= number_to_currency @order.total, locale: :de
      %p to:

      %P An e-mail with these instructions and an invoice has also been sent to you.
